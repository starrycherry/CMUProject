package portletUtil;

import java.util.Comparator;

public class PortletComparator implements Comparator<PortletInfo> {
	@Override
	public int compare(PortletInfo arg0, PortletInfo arg1) {
		return arg0.getName().compareTo(arg1.getName());
	}
}
